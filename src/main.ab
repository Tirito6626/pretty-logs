#!/bin/env amethyst
pub import {
    update_format,
    update_error_format, 
    update_warn_format, 
    update_log_format,
    update_success_format
} from "lib/misc/update_format.ab"

pub import * from "lib/misc/colors.ab"

pub import { error, error_exit, error_fail } from "lib/error.ab"
pub import { warn } from "lib/warn.ab"
pub import { log } from "lib/log.ab"
pub import { success } from "lib/success.ab"
pub import { throw } from "lib/throw.ab"

update_format();